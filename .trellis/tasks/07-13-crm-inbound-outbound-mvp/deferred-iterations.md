# CRM 入库与出货 MVP 后续迭代追踪

## Purpose

本文件区分本任务的确认延期范围与尚未完成的当前验收工作。它保留
依赖和验收前提，不将未来需求混入当前 MVP 的交付边界。

## Traceability Rules

- `Deferred Items` 中的项目不属于当前任务的验收失败项。
- 延期项目开始前必须创建独立任务，并补齐 PRD、技术设计、实施计划和测试计划。
- `Remaining Work In Current Scope` 是当前任务继续实施和验收的唯一清单；任务在这些项目完成前保持 `in_progress`。

## Deferred Items

| ID | Deferred Scope | Reason | Dependencies | Future Deliverables |
|---|---|---|---|---|
| D-001 | 工艺卡、坯布投产与坯布到成品转换 | PRD 明确不在首版实现范围 | 一匹坯布的追溯口径、客户和工艺要求 | 工艺卡领域模型、投产/产出流程、转换损耗规则和 E2E |
| D-002 | 日常成品入库页面 | PRD 仅保留历史导入及后续衔接边界 | 工艺卡或独立成品入库业务口径 | 成品入库单据、工艺卡关联和台账写入规则 |
| D-003 | 库存调整单 | PRD 的 follow-up backlog | 调整原因、审批与审计口径 | 调整单据、变动类型、权限和余额回放测试 |
| D-004 | 用户可重复操作的历史导入/重导和数据清洗页面 | 首版只允许受控命令行首次导入 | 导入批次回滚、清洗工作流和操作权限 | 导入管理 UI、失败明细、批次回滚和清洗流程 |

## Remaining Work In Current Scope

| ID | Remaining Scope | Current Status | Blocker / Next Action |
|---|---|---|---|
| R-001 | 完成受控历史 Excel 导入 | 已完成隔离验证 | 已支持真实表头、静态库存快照、匹数/米数、同一来源行双向变动、负数出货与仅米数历史行；仍可后续优化同单号历史明细归并。 |
| R-002 | 迁移对账期初 | 已完成隔离验证 | 在首次导致负库存的库存键前写入 `MIGRATION_RECONCILIATION_OPENING`；实际导入后两类库存余额均无负数。 |
| R-003 | 导入器自动化测试与真实对账演练 | 已完成隔离验证 | 新增 6 个导入器测试；隔离库 dry-run 与实际导入均通过，来源行 3,949、台账 3,850、待清洗 3,531、对账期初 501。 |
| R-004 | 前端筛选与规格联想 | 已完成构建验证 | 单据列表已接入日期、加工单位、收货单位和单号筛选；编辑器已接入 `/inventory/suggestions` 的品名、品号、含毛量、颜色和缸号联想。 |
| R-005 | 前端运行与 E2E 验收 | 已完成 | 使用可清理的 `aiadmin_test` 运行真实 Chromium 场景；登录、主数据、坯布入库、余额追溯、软删除/恢复和成品出货扣减全部通过，库存 E2E `3 passed`。 |
| R-006 | 后端测试覆盖 | 已完成 | 新增历史单据不可编辑/删除/恢复、导入失败全量回滚、迁移对账期初来源/数量/按库存键非负余额断言；库存 API `22 passed`、导入器 `18 passed`。 |
| R-007 | 任务质量收尾 | 已完成 | 完整后端测试 `137 passed, 1 skipped`；mypy、ty、Ruff、Ruff format、前端 lint/build 和库存 E2E 均通过。`backend/scripts/lint.sh` 在当前 Windows/WSL 环境因找不到 `uv` 未直接运行，已用等价 `uv run` 命令完成检查。 |

## Suggested Continuation Order

1. 完成规格回顾并提交本任务相关代码与测试。
2. 归档当前任务；后续 D-001 至 D-004 继续通过独立任务管理。

## Carry-Forward Acceptance Notes

- 不能用 Excel 的静态库存列覆盖系统余额；它仅能作为来源快照和对账依据。
- 迁移对账期初只能由导入器创建，不能暴露为日常调整或单据操作。
- 新建业务单不得接受 `未填写品号`、`未填写含毛量`、`未分缸` 等历史占位值。
- 在真实 Excel 演练通过前，不能将当前导入器用于生产或本地正式数据。
