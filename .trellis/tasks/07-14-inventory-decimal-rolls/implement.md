# 匹数小数精度实施计划

1. 新增迁移，将匹数存储和台账变动转换为 `NUMERIC(18,2)`，并定向修复来源 JSON 可验证的小数历史行及其对账期初；实现受保护的降级检查。
2. 更新库存 ORM、写入/读取 DTO、服务层余额计算和导入器，使用 `Decimal` 并分离读取与写入校验。
3. 扩展库存 API 与导入器测试，覆盖 `0.50`、超精度拒绝、历史列表 200、定向修复后的余额和非负不变量。
4. 生成 OpenAPI 客户端，更新前端匹数输入精度，并验证成品出货、坯布台账、库存余额页面。
5. 在当前本地数据库执行迁移后，核对受影响历史记录、台账和余额；运行完整质量门槛并记录迁移前后结果。

## Validation Record

- 已备份迁移前数据库，并在当前数据库执行 revision `3b6f74e6d931`。
- 已核对历史单号 `0005290`、`0005348` 的明细为 `0.50`、台账变动为
  `-0.50`，对应迁移对账期初各增加 `+0.50`；余额没有负值。
- 成品出货历史列表接口返回 200。空验证数据库迁移到 head 后，库存 API 与
  导入器回归测试共 21 项通过；mypy、ty、Ruff、前端 lint 与 build 通过。
- Playwright E2E 在主数据下拉/AutoComplete 交互等待处超时；用户要求跳过，
  因此不作为本次交付阻断项。
- 日期修复迁移 `4d7e8f9a0b1c` 已在数据库副本和实际业务库执行；Excel 序列号
  `46090` 已恢复为 `2026-03-09`，年度结存按对应年份 `12月31日` 处理。
- 日期修复前已备份实际数据库；实际库中错误的 `2026-07-14` 日期计数为 0，
  单据与对应台账日期不一致数为 0，成品出货列表接口仍返回 200。
